name: Deploy Netflix Clone

on:
  push:
    branches:
      - main

jobs:
  deploy-backend:
    name: Deploy Backend to Render
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Trigger Render Deployment
      run: |
        echo "✅ Code pushed to main branch"
        echo "🚀 Render will auto-deploy from GitHub"
        echo "📊 Monitor at: https://dashboard.render.com"
        
  notify-completion:
    name: Deployment Complete
    runs-on: ubuntu-latest
    needs: [deploy-backend]
    if: always()
    steps:
      - name: Log Deployment Status
        run: |
          echo "✅ Code pushed successfully"
          echo "📝 Vercel: Auto-deploys from GitHub (no action needed)"
          echo "📝 Render: Auto-deploys from GitHub (no action needed)"
          echo "📊 Check Vercel: https://vercel.com/dashboard"
          echo "📊 Check Render: https://dashboard.render.com"
